{
  "name": "zk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "cd scripts && chmod +x compile.sh && ./compile.sh",
    "compile:all": "npm run compile:username_hash && npm run compile:merkle_path && npm run compile:merkle_inclusion && npm run compile:merkle_update && npm run compile:username_merkle",
    "compile:hello": "mkdir -p build && circom circuits/hello.circom --r1cs --wasm --sym -o build -l node_modules",
    "compile:username_hash": "mkdir -p build && $(which circom) circuits/username_hash.circom --r1cs --wasm --sym -o build -l node_modules",
    "compile:merkle_path": "mkdir -p build && circom circuits/merkle/merkle_path.circom --r1cs --wasm --sym -o build -l node_modules",
    "compile:merkle_inclusion": "mkdir -p build && circom circuits/merkle/merkle_inclusion.circom --r1cs --wasm --sym -o build -l node_modules",
    "compile:merkle_update": "mkdir -p build && circom circuits/merkle_update.circom --r1cs --wasm --sym -o build -l node_modules",
    "compile:username_merkle": "mkdir -p build && circom circuits/username_merkle.circom --r1cs --wasm --sym -o build -l node_modules"
  },
  "keywords": [],
  "author": "alien-protocol",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^25.3.0",
    "snarkjs": "^0.7.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7"
  }
}
